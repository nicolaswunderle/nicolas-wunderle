---
import Layout from "../layouts/Layout.astro";
import Portfolio from "../components/Portfolio";
import { getCollection } from "astro:content";
const works = await getCollection("works");
const filters = await getCollection("portfolioFilters");
---

<Layout
  title="Portfolio - Nicolas Wunderle"
  description="Découvrez ma passion pour l'audiovisuel au travers de mon portfolio."
>
  <Portfolio portfolio={works} filters={filters} client:only />
</Layout>
