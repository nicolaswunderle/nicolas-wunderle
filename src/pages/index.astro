---
import Layout from "../layouts/Layout.astro";
import "slick-carousel/slick/slick.css";
import "slick-carousel/slick/slick-theme.css";
import { getCollection } from "astro:content";
import SkillCard from "../components/SkillCard";
import Sidebar from "../components/Sidebar";
const personalInfo = await getCollection("personalInfo");
const skills = await getCollection("skills");
---

<Layout
  title="À propos - Nicolas Wunderle"
  description="Bostami Tailwind CSS Personal Portfolio HTML Template"
>
  <!-- about me section start -->
  <div class="pt-12 md:py-12 px-2 sm:px-5 md:px-10 lg:px-14">
    <!-- personal info for mobile devices start -->
    <div class="lg:hidden">
      <Sidebar personalInfo={personalInfo} />
    </div>
    <!-- personal info for mobile devices end -->
    <!-- about page title -->
    <h2
      class="relative inline-block text-[2.5rem] dark:text-white font-bold transform after:absolute after:md:w-[12rem] after:left-[22.5rem] after:h-0.5 after:bg-gradient-to-r after:from-btn-secondary after:to-btn-secondary after:content-[''] after:rounded-md after:top-2/4 after:transform"
    >
      À propos de moi
    </h2>
    

    <div
      class="lg:grid grid-cols-12 md:gap-10 pt-4 md:pt-[30px] items-center"
    >
      <div class="col-span-12 space-y-2.5">
        <div 
          class="lg:mr-16 text-text-primary dark:text-main-text leading-7 [&>p]:mb-3 [&>h4]:mt-10 [&>h4]:text-[initial]"
          set:html={personalInfo[0].body}
        >
          
        </div>
        <div></div>
      </div>
    </div>
  </div>

  <!-- what i do section start -->
  <div class="pb-12 px-2 sm:px-5 md:px-10 lg:px-14">
    <h3 class="text-[2.2rem] dark:text-white font-bold font-robotoSlab pb-5">
      Mes passions
    </h3>
    <div class="grid gap-8 grid-cols-1 md:grid-cols-2 xl:grid-cols-2">
      {
        skills.map((item) => {
          return <SkillCard skill={item} />;
        })
      }
    </div>
  </div>

  <!-- what i do section End -->

  <!-- about me section End -->
</Layout>
